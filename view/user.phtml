<div class="content">
    <?php //print_r($this->content); ?>

    <table id="usertable">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Voornaam</th>
                <th>Achternaam</th>
                <th>Wachtwoord</th>
                <th>Acties</th>
            </tr>
        </thead>
        <tbody></tbody>
    </table>
</div>

<script>
    var json = <?=$this->content;?>;
    var tbody = $("#usertable tbody");
    var tr = "";

    // elk object in de json
    $.each( json, function( key, val ) {
        //console.log(val);

        tr += "<tr>";

        // elke value van de bijbehorende key in het object
        $.each(val, function( key2, val2 ) {
            tr += "<td>"+val2+"</td>";
        });

        tr += "<td><a href='<?php echo $this->baseDir; ?>user/edit/" + val.id + "'>EDIT</a></td></tr>";
        tbody.append(tr);
        tr = "";
    });
</script>
